#pragma once
#include "inventory_srv.h"
#include "warehouse_srv.h"
#include "product_srv.h"


class InventoryApplication
{
private:
    /* data */
public:
    InventoryApplication(/* args */);
    ~InventoryApplication();

    // Result adjustInventory(AdjustmentRequest);
    // Result queryStock(std::string warehouse_name, std::string product_name);
    // Result createProduct(ProductRequest);
    // Result createWarehouse(WarehouseRequest);

};

